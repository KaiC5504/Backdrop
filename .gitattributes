# Normalise to LF in the repo. Windows CRLF leaking into shell scripts or YAML breaks
# builds on macOS in ways that are painful to diagnose remotely.
* text=auto eol=lf

*.png binary
*.jpg binary
*.mp4 binary
*.mov binary
